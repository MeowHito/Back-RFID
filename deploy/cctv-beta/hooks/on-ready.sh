#!/bin/sh
# Called by MediaMTX runOnReady hook (path becomes ready for playback).
# Args: $1 = MTX_PATH
# Currently we don't act on this — kept as a no-op placeholder so MediaMTX doesn't error.
set -e
exit 0
